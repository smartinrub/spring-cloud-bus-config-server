spring:
  application:
    name: config-server-client
  cloud:
    config:
      uri: "http://localhost:8888"
      profile: development
management:
  endpoints:
    web:
      exposure:
        include: "refresh"
