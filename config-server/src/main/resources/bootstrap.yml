# Bug: https://github.com/spring-cloud/spring-cloud-config/issues/1490
encrypt:
  key: mykey
  fail-on-error: false
spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        encrypt:
          enabled: true
