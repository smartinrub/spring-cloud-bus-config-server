server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: "${HOME}/workspace/sergio/config-server-props"
          refresh-rate: 5 # the config server will fetch updated configuration every 5 seconds

